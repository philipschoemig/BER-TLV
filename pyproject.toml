[tool.poetry]
name = "bertlv"
version = "0.2.3"
description = "Library for BER TLV (tag-length-value) en-/decoding."
license = "BSD-3-Clause"
authors = ["Philip Schömig <philip.schoemig@posteo.de>"]
packages = [
    { include = "bertlv", from = "src" },
]

[tool.poetry.dependencies]
python = "^3.6"
anytree = "^2.8.0"

[tool.poetry.dev-dependencies]
pytest = "^5.3.5"
pytest-cov = "^2.8.1"
tox = "^3.14.5"
bump2version = "^1.0.0"
black = "^19.10b0"
flake8 = "^3.8.3"
pylint = "^2.4.4"
doc8 = "^0.8.0"
sphinx = "^2.4.4"
bandit = "^1.6.2"
flake8-bugbear = "^20.1.4"

[tool.poetry.scripts]
bertlv = 'bertlv.__main__:main'

[tool.black]
line-length = 88
target-version = ["py36"]
include = '\.pyi?$'
exclude = '''

(
  /(
      \.eggs         # exclude a few common directories in the
    | \.git          # root of the project
    | \.hg
    | \.mypy_cache
    | \.tox
    | \.venv
    | _build
    | buck-out
    | build
    | dist
  )/
  | foo.py           # also separately exclude a file named foo.py in
                     # the root of the project
)
'''

[build-system]
requires = ["poetry>=1.0"]
build-backend = "poetry.masonry.api"
